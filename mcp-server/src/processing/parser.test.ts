// Tests for parser.ts - deprecated (processing moved to claude -p)
// This file can be safely deleted.
import { describe, it, expect } from "bun:test";

describe("parser (deprecated)", () => {
  it("is no longer used", () => {
    expect(true).toBe(true);
  });
});
